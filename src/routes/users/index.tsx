// src/routes/users/index.tsx

const UserList = () => {
  return (
    <section className="user-list">
      <h2>User Management</h2>
      <p>This will be the hub for managing user accounts and roles.</p>
    </section>
  );
};

export default UserList;
