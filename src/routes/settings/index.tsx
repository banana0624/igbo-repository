// src/routes/settings/index.tsx

const AdminSettings = () => {
  return (
    <section className="admin-settings">
      <h2>Admin Settings</h2>
      <p>Manage application preferences and configurations here.</p>
    </section>
  );
};

export default AdminSettings;
